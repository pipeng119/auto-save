import { AutoSaveFormDirective } from './auto-save-form.directive';

describe('AutoSaveFormDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoSaveFormDirective();
    expect(directive).toBeTruthy();
  });
});
